# Direct dependencies, pinned exactly.
#
# These were unbounded (">=") until a rebuild silently pulled google-genai 2.19,
# whose stricter response_schema validation killed every LLM call in the app
# with no code change on our side. An app is deployed, not imported, so the
# reproducible build is worth more here than automatic upgrades.
#
# requirements.lock.txt holds the FULL resolved set, transitive packages
# included, and is what the Dockerfile installs -- pinning only the direct
# dependencies would have left pydantic, protobuf and the rest free to drift.
#
# To upgrade: relax the pin you want moved, rebuild, run the test suite, then
# regenerate the lock with `pip freeze` from the rebuilt image, sorted, into
# backend/requirements.lock.txt.

fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pandas==3.0.5
openpyxl==3.1.5
google-auth==2.56.3
google-auth-oauthlib==1.4.0
google-api-python-client==2.198.0
itsdangerous==2.2.0
google-genai==2.19.0
openai==3.3.1
jsonschema==4.26.0
python-dotenv==1.2.3
python-calamine==0.8.2
pyarrow==25.0.1
mcp==2.0.0
rank_bm25==0.2.2
docker==7.2.0
neo4j==6.2.0
statsmodels==0.14.6
python-docx==1.2.0
python-pptx==1.0.2
matplotlib==3.11.1
pytest==9.1.1
